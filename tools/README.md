# tools

A collection of useful tools for RZ platforms.

## binmake

This directory contains scripts to convert platform information JSON files into binary images.

## bpgen

This directory contains a unified tool to generate boot parameter binaries (`bl2_bp.bin`) for RZ devices. The boot parameter is a small metadata sector that the Boot ROM reads before loading BL2.  